[Unit]
Description=Per-folder daily backups (Cockpit plugin)

[Timer]
# Placeholder: apply-schedule writes a drop-in with one OnCalendar entry
# per configured backup time, replacing this default.
OnCalendar=*-*-* 03:00:00
AccuracySec=1min
Persistent=true

[Install]
WantedBy=timers.target
